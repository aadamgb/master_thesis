\resizebox{0.4\textwidth}{!}{%
\begin{tikzpicture}[
    x={(1.5cm, 0.4cm)},
    y={(0cm, 1cm)},
    z={(0.6cm, -0.2cm)},
    line cap=round, line join=round
]
    % --- Both planes share z=0, standing vertically ---

    % Outer Plane
    \coordinate (A) at (-2,  3, 0);
    \coordinate (B) at ( 2,  3, 0);
    \coordinate (C) at ( 2, -3, 0);
    \coordinate (D) at (-2, -3, 0);

    % Inner Plane (centered, smaller, same plane)
    \coordinate (A_in) at (-1,  1.5, 0);
    \coordinate (B_in) at ( 1,  1.5, 0);
    \coordinate (C_in) at ( 1, -1.5, 0);
    \coordinate (D_in) at (-1, -1.5, 0);

    % Points
    \coordinate (P2) at (-1.5,  1, 0);
    \coordinate (P1) at ( 0.75, 0.5, 0);

    % Gate center (origin of gate frame)
    \coordinate (O) at (0, 0, 0);

    % --- DRAWING ---
    % Outer Plane (blue tint)
    \draw[thick, fill=skyBlue, fill opacity=0.7, dashed]
        (A) -- (B) -- (C) -- (D) -- cycle;
    \node[left, font=\small] at (-2, 0, 0) {$\Pi_{\mathrm{outer}}$};

    % Inner Plane (orange tint)
    \draw[thick, fill=green!25, fill opacity=0.9]
        (A_in) -- (B_in) -- (C_in) -- (D_in) -- cycle;
    \node[below right, font=\small] at (C_in) {$\Pi_{\mathrm{inner}}$};

    % --- Gate frame axes ---
    % Forward axis (f_g): pointing out of the plane, along z-axis in tikz coords
    \draw[-{Stealth[length=6pt]}, thick, myred]
        (O) -- ++(0, 0, 2.4)
        node[right, font=\small, myred] {$\hat{\mathbf{g}}_1$};

    % Right/lateral axis (r_g): pointing right, along x-axis in tikz coords
    \draw[-{Stealth[length=6pt]}, thick, mygreen]
        (O) -- ++(1.5, 0, 0)
        node[right, font=\small, mygreen] {$\hat{\mathbf{g}}_2$};

    % Up axis (g_3): pointing up, along y-axis in tikz coords
    \draw[-{Stealth[length=6pt]}, thick, myblue]
        (O) -- ++(0, 2.2, 0)
        node[right, font=\small, myblue] {$\hat{\mathbf{g}}_3$};

    % Gate center dot
    \filldraw[black] (O) circle (1.5pt);

    % Points
    \filldraw[black] (P1) circle (1.5pt)
        node[above, yshift=3pt] {$P_1$};
    \filldraw[red]   (P2) circle (1.5pt)
        node[above, yshift=3pt] {$P_2$};

\end{tikzpicture}
}
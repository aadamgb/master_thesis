\documentclass{standalone}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\definecolor{tabblue}{rgb}{0.0000, 0.4470, 0.7410}
\definecolor{taborange}{rgb}{0.8500, 0.3250, 0.0980}
\begin{document}
\begin{tikzpicture}

% ---- Main axis (steps) ----
\begin{axis}[
    name=plot1,
    width=7.5cm,
    height=6cm,
    grid=both,
    minor grid style={dashed, gray!30},
    major grid style={dashed, gray!30},
    minor tick num=1,
    xlabel={x [m]},
    ylabel={y [m]},
    legend pos=south east,
    % colormap/viridis,
    % unbounded coords=jump,
    % colorbar,
    % colorbar style={ylabel={$\|v\|$ [m/s]}}
]

% \addplot[
%     scatter,
%     only marks,
%     mark=*,
%     mark size=0.6pt,
%     scatter src=explicit,
% ]
% table[
%     x=p_x,
%     y=p_y,
%     meta expr={sqrt(\thisrow{v_x}^2 + \thisrow{v_y}^2 + \thisrow{v_z}^2)},
%     col sep=comma,
% ] {data/aos_refined_traj.csv};

\addplot[
    dashed, 
    line width=1.0pt,
    ]
table[x=p_x, y=p_y, col sep=comma] {data/aos_refined_traj.csv};

\end{axis}


\end{tikzpicture}
\end{document}
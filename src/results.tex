%!TEX root = ../main.tex

\chapter{Results \label{chap:results}}

In this chapter, the results of the proposed differentiable simulator are presented and evaluated.
First, the assessment metrics used throughout the chapter are defined in \refsec{sec:eval_criteria}.
\todo{validation of the simulator}
Subsequently, the performance of the proposed method is compared against standard model-free \ac{RL} training and a classical fixed-gain geometric controller in \refsec{sec:baseline}.
Qualitative results, including trajectory visualizations, velocity profiles, and the transferability of the trained policiesare are presented.
An ablation study is then conducted in \refsec{sec:ablation} to evaluate the effect of key design choices.
Finally, preliminary real-world validation results are presented in \refsec{sec:realworld}.

\todo{Update this introduction, since I changed the section names....}
\section{Evaluation Criteria}\label{sec:eval_criteria}

This section details the metrics and experimental framework used to evaluate the performance of the proposed differentiable physics approach against a model-free \ac{PPO}. 
The evaluation is structured around three primary axes: training efficiency, performance  in terms of lap time and success rate, and sim-to-sim transferability. 
% Furthermore, a specific case study is conducted within the differentiable simulation framework to isolate the effects of adaptive versus fixed controller gains. 
% In this study, a policy trained using the \ac{LVHR} control abstraction with static gains is compared against an augmented formulation where the policy dynamically modulates adaptive gains $\{k_v, k_R, k_\omega\}$, as described in \autoref{sec:diff-opt}. 
% This comparison quantifies the benefit of online stiffness modulation and demonstrates the utility of our simulator for complex control optimization.

\subsection{Sample Efficiency}
One of the central claims of this thesis is that leveraging analytical gradients from differentiable simulation significantly improves sample efficiency compared to standard model-free \ac{RL} algorithms.
To evaluate this claim, the training convergence of the proposed differentiable physics approach is compared against \ac{PPO} trained on the same task, observation space, network architecture, and control abstraction.
Sample efficiency is measured as the wall-clock training time and the number of environment steps required for each method to converge to a stable policy.
Convergence is defined as the point at which the cumulative loss/reward ceases to improve by more than a threshold.
For a fair comparison, the randomized processes use a common seed during the training process.


\subsection{Perfromance}
\subsubsection{Lap Time and Succes Rate}

Ultimately, the primary objective of \ac{ADR} is to traverse a sequence of gates as fast as possible.
The two metrics used to evaluate this objective are the lap time $T_f$ and the \ac{SR}.

The lap time $T_f$ is defined as the time elapsed from the start of the episode until the drone passes the final gate.
The success rate is defined as the ratio of successfully passed gates $G_p$ to the total number of gates $G$ in the racing track,

\begin{equation}
    \mathit{SR} = \frac{G_p}{G} \in [0, 1].
\end{equation}

A gate $g$ is characterized by its position $\mathbf{p}_g \in \mathbb{R}^3$, orientation $\mathbf{R}_g \in SO(3)$, and dimensions $(t,\, w,\, h)$, corresponding to the thickness, width, and height of the gate opening, respectively.
The gate frame axes are defined as,
\begin{figure}[H]
    \centering
    \include{fig/tikz/planes}
    \caption{Gate traversal geometry. The success region (green) is defined with respect to the gate position $\mathbf{p}_g$, orientation $\mathbf{R}_g$ and dimensions $(t, w, h)$. \todo{add gate dimension to the plot, t,w,h}}
    \label{fig:gate_geometry}
\end{figure}

\begin{equation}
    \hat{\mathbf{g}}_1 = \mathbf{R}_g \hat{\mathbf{e}}_1, \qquad
    \hat{\mathbf{g}}_2 = \mathbf{R}_g \hat{\mathbf{e}}_2, \qquad
    \hat{\mathbf{g}}_3 = \mathbf{R}_g \hat{\mathbf{e}}_3,
\end{equation}

\noindent where $\hat{\mathbf{g}}_1$, $\hat{\mathbf{g}}_2$, and $\hat{\mathbf{g}}_3$ denote the forward (normal), lateral , and vertical axes of the gate, respectively.
The position of the drone relative to the gate, expressed in the gate frame, is,

\begin{equation}
    \mathbf{p}^g_k = \mathbf{R}_g^\top \left( \mathbf{p}_k - \mathbf{p}_g \right) =
    \begin{bmatrix} p^g_1 & p^g_2 & p^g_3 \end{bmatrix}^\top.
\end{equation}

\noindent A gate $g$ is considered successfully passed at timestep $k$ if,

\begin{equation}
    % \mathit{passed} \iff
    \left| p^g_1 \right| < \frac{t}{2}
    \quad \wedge \quad
    \left| p^g_2 \right| \leq \frac{w}{2}
    \quad \wedge \quad
    \left| p^g_3 \right| \leq \frac{h}{2},
\end{equation}
and a gate is unsuccessfully passed when,

\begin{equation}
    % \mathit{crash} \iff
    \left| p^g_1 \right| < \frac{t}{2}
    \quad \wedge \quad
    \left( \left| p^g_2 \right| > \frac{w}{2}
    \;\vee\;
    \left| p^g_3 \right| > \frac{h}{2} \right).
\end{equation}

\subsubsection{Root Mean Square Error}
When a referene path is used, another performance metric utilized is to compute the \ac{RMSE} between the drone's position and the reference path.


\subsection{Transferability and Robustness}\label{subsec:transfer}

Learned policies are prone to overfitting to the training environment and may fail to generalize when deployed under conditions that deviate from those seen during training, due to model mismatches and unmodeled effects.
To mitigate this, \ac{DR} is employed during training as described in \autoref{subsec:DR}.

To assess transferability, the policies trained in the proposed differentiable simulator are evaluated in a high-fidelity simulation environment using the MRS UAV stack in Gazebo~\cite{baca_mrs_2021}.
Since the differentiable physics and model-free \ac{RL} policies are optimized under different paradigms, the sim-to-sim performance gap is analyzed for each method, measuring the degradation in success rate $\mathit{SR}$ and lap time $T_f$ relative to the results obtained in the training simulator.
Additionally, robustness to external disturbances is evaluated by exposing the trained policies to wind perturbations during deployment, without any retraining or fine-tuning.


% \begin{figure}[H]
%   \centering
%   \includegraphics[width=1.0\textwidth]{./fig/plots/throttle-val/hypotheses.pdf}
%   \caption{Yaw response to unit step, variable amblitude and variable frequency signals.}
%   \label{fig:throttle-val}
% \end{figure}

% Finally, the throttle response


% \begin{figure}[H]
%     \centering
%     \includegraphics[width=1.0\textwidth]{fig/photos/placehldrs/sim-val2.png}
%     \caption{\todo{This is a placeholder figure, (I need to get the actual data)Velocity response in our sim and gazebo}}
%     \label{fig:vel_resp}
% \end{figure}







\section{Control Abstraction Evaluation}\label{sec:ablation}
In this section, we evaluate how the implemented control abstractions affect the sample efficiency, racing performance, as well as the transferability and robustness of the learned control policies.

\subsection{Sample Efficency}
To evaluate sample efficiency, the four policies are trained for 40 million steps under identical conditions, including the same randomization settings, loss function, network architecture, learning rate, and hyperparameters.
We then analyze how the loss evolves throughout training.


\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/sample-eff-ca/hypotheses.pdf}
    \caption{Loss verus time step for the different control abstractions.}
    \label{samp-eff-ca}
\end{figure}

From \autoref{samp-eff-ca}, it can be observed that, as discussed in \autoref{met:ca}, the \ac{SRT} policy requires the longest training time to converge, since the network must implicitly learn the mapping between motor commands and body rates.
Although this approach is slower to train, it has the advantage of requiring no controller gains or manual tuning.
In contrast, the \ac{CTBR} policy converges significantly faster, reaching convergence after approximately 1.5 million steps.
The geometric control abstractions, \ac{LVHR} and \ac{LVHR}+g, converge even faster, requiring only around 0.5 million steps.
Despite their improved convergence speed, its woth noting that latter policies rely on carefully tuned gains to ensure stable behavior, which can be time consuming and task-dependant.

% \todo{Plot gradients of backward and foorward passes to comare their quality..}

\subsection{Performance}
To evaluate performance, the policies are further trained on a specific racing track.
The selected track is commonly used in the literature, as it contains straight sections, high-curvature segments, and a split-S gate configuration, making it suitable for benchmarking agile flight performance.
This circuit is commonly referred to as the UZH track, as it was designed by the University of Zurich.

To generate trajectories passing through the gates, the time-optimal gate traversal planner proposed in~\cite{qin_time-optimal_2024} is used.
The planner generates trajectories that account for both the gate geometry and the quadrotor dynamics.
Training directly on the optimal trajectory consistently resulted in failure to track the reference.
We hypothesize that the optimization becomes trapped in local minima, preventing the policy from converging to a stable solution.
To facilitate learning, a curriculum-based training strategy is adopted.
Initially, the trajectory speed is scaled down to 10\% of the original reference speed.
Once an episode achieves an RMSE below $0.15$~m, the trajectory speed is increased by an additional 10\%.
Using this approach, the policies are able to track the trajectory at up to 60\% of the original speed, which remains significantly below the optimal performance.
This limitation requires further investigation, particularly regarding the design of the loss function and the neural network architecture.

\autoref{tab:uzh_ca} and \autoref{fig:uzh_ca} present the tracking results of the four control abstractions on the UZH track at 60\% of the reference speed, where the policies achieve a maximum speed of 10 m/s.
\begin{table}[H]
    \centering
    \caption{Position and Velocity \ac{RMSE} of the control abstractions, mean $\pm$ std over 5 runs.}
    \label{tab:uzh_ca}
    \begin{tabular}{ccccc}
    \hline
RMSE  & SRT  & CTBR & LVHR & LVHR+g \\ \hline
Position [m]  & $0.225\pm 0.031$ & $0.334\pm 0.008$ & $\mathbf{0.132\pm 0.003}$ & $0.201\pm 0.003$  \\
Velocity [m/s]  & $0.190\pm 0.002$ & $\mathbf{0.180\pm 0.002}$ & $0.219\pm 0.027$ & $0.224\pm 0.008$ \\ \hline
\end{tabular}
\end{table}


The results presented in \autoref{fig:uzh_ca} and \autoref{tab:uzh_ca} indicate that the \ac{LVHR} control abstraction achieves the lowest position tracking error, while \ac{CTBR} obtains the lowest velocity tracking error.
At this suboptimal speed, all policies achieve a \ac{SR} of 100\%, with no gate collisions occurring during the experiments.

Furthermore, the policies exhibit broadly similar behavior.
The collective thrust commands follow comparable patterns across all control abstractions.
The \ac{SRT} and \ac{LVHR}+g policies appear to lag slightly behind the reference trajectory, whereas the \ac{CTBR} policy tends to move ahead of the reference and produces the most oscillatory control commands.
Although the \ac{LVHR} policy achieves the best position tracking performance overall, its velocity tracking error increases significantly toward the end of the trajectory.
\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/uzh-track/hypotheses.pdf}
    \caption{Tracking error of the different control abstractions for the UZH racing track.}
    \label{fig:uzh_ca}
\end{figure}

% \begin{table}[H]
%     \centering
%     \caption{Position and Velocity \ac{RMSE} of the control abstractions, mean $\pm$ std over 5 runs.}
%     \label{tab:uzh_ca}
%     \begin{tabular}{ccccc}
%     \hline
% RMSE  & SRT  & CTBR & LVHR & LVHR+g \\ \hline
% Position [m]  & $0.225\pm 0.031$ & $0.334\pm 0.008$ & $\mathbf{0.132\pm 0.003}$ & $0.201\pm 0.003$  \\
% Velocity [m/s]  & $0.190\pm 0.002$ & $\mathbf{0.180\pm 0.002}$ & $0.219\pm 0.027$ & $0.224\pm 0.008$ \\ \hline
% \end{tabular}
% \end{table}

Analyzing the commands of each control abstraction individually reveals information in policy behavior.
For the \ac{SRT} policy, in fig:srt-action it can be seen that the rotor commands follow a trend similar to the collective thrust profile shown in \autoref{fig:uzh_ca}.
The policy successfully learns to rotate the quadrotor by generating differential thrust between the rotors.
Around $12$~s, high-frequency oscillations can be observed in the commands; this interval corresponds to the split-S section of the track, where aggressive rotational maneuvers are required.

\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/commands-srt/hypotheses.pdf}
    \caption{Commanded single rotor thrusts for the UZH trajectory.}
    \label{fig:srt-action}
\end{figure}

From \autoref{fig:ctbr-action}, it can be observed that the control commands are relatively jittery.
The pitch and roll moments are the most aggressive, reaching peak values of approximately $0.05~\mathrm{N\cdot m}$ during the split-S maneuver.
In contrast, the yaw moments remain close to $0.0~\mathrm{N\cdot m}$, since maintaining the heading aligned with the gates was not explicitly prioritized during training.

The platform has a maximum \ac{TWR} of 6.9, corresponding to a maximum thrust of approximately $82~\mathrm{N}$.
For the evaluated trajectory, the maximum commanded thrust is around $14~\mathrm{N}$, which corresponds to only $17\%$ of the maximum available thrust.

\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/commands-ctbr/hypotheses.pdf}
    \caption{Commanded collective thrust and body rates for the UZH trajectory.}
    \label{fig:ctbr-action}
\end{figure}

The velocity and heading-rate commands shown in \autoref{fig:lvhr-action} exhibit a similar structure throughout the trajectory.
The commanded velocities along the body $x$ and body $y$ axes have the largest magnitudes, while the heading rate and vertical velocity $v_z$ remain close to zero for most of the flight.

The main advantage of this control abstraction is the smoothness of the generated commands.
Additionally, as shown in \autoref{samp-eff-ca}, this policy achieves the fastest convergence during training.
\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/commands-lvhr/hypotheses.pdf}
    \caption{Commanded linear velocities and heading rate for the UZH trajectory.}
    \label{fig:lvhr-action}
\end{figure}

Finally, in the current setup, augmenting the policy to output the gains of the geometric controller does not appear to provide significant improvements.
The gains are initialized with the same values used in the \ac{LVHR} policy, namely $k_v = 0.6$, $k_R = 0.2$, and $k_\omega = 0.05$.
However, in this case, the policy outputs scaling factors between $0$ and $1$ that modulate these gains.

As shown in \autoref{fig:lvhr-g-action}, the gains remain relatively constant throughout the trajectory, raising the question of whether the feature justified in the current setup.
Nevertheless, it is difficult to draw definitive conclusions from this experiment, since the evaluated trajectory remains far from optimal and the actuators operate well below saturation for most of the flight.

Further investigation will be conducted in future work, as we believe this approach could be beneficial in more aggressive flight regimes or under varying operating conditions, potentially providing advantages over fixed-gain geometric controllers.
As discussed in~\cite{boufrioua_gain_2022}, ``it is widely known that this controller design approach provides better stability and performance results for slowly varying parameters''~\cite{rugh_analytical_1991}.

\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/commands-lvhr+g/hypotheses.pdf}
    \caption{Commanded linear velocities, heading rate and controller gains for the UZH trajectory.}
    \label{fig:lvhr-g-action}
\end{figure}



\subsection{Transferability}


To evaluate how the controllers transfer to a high-fidelity simulator, we implemented a neural nework controller as a ROS node in C++ and integrated into the MRS UAV system~\cite{baca_mrs_2021} within Gazebo.
The \ac{CTBR} policy was selected for evaluation, as this control abstraction is widely used in aerial robotics and is currently the only one supported by the MRS UAV Pixhawk~4~\cite{mrs_uav_px4} \ac{API}.
In future work, the remaining control abstractions will also be implemented within the PX4 interface, particularly the \ac{LVHR}+g approach.
Nevertheless, the \ac{CTBR} controller provides a suitable baseline for evaluating transferability.

Using this setup, we compare the position and velocity \ac{RMSE} obtained in our simulator against the results in Gazebo.
In Gazebo, two configurations are considered: one using ground-truth state estimation and another using a more realistic state estimator representative of real flight conditions.

\section{Surrogate Gradient Analysis}
In this section, we analyze the effect of using surrogate gradients during the policy optimization process.
The gradient is backpropagated from the loss function to the policy parameters through the chain rule.
As discussed in \autoref{sec:diff-opt}, the gradients are computed numerically from the computational graph.
Certain operations, such as \autoref{eq:motspd}, can be highly sensitive to small perturbations, leading to unstable gradients and making the optimization process more difficult.

This behavior can be observed in \autoref{fig:gradients}, where the loss and gradient norm are plotted against the training steps for two different approaches: a simplified backpropagated model that omits motor dynamics, and the full model including the motor dynamics.
At the beginning of training, both approaches exhibit similar behavior.
However, the full model eventually converges to a suboptimal solution and fails to track the trajectory successfully.
By analyzing the gradient norm of the full model, larger oscillations can be observed, which we believe are responsible for the unstable optimization and suboptimal convergence.
\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/gradients/hypotheses.pdf}
    \caption{Gradients.}
    \label{fig:gradients}
\end{figure}

\subsection{Robustness}

\section{Real World Validation}\label{sec:realworld}

On the 16th of April 2026, during the MRS testing campaign, we conducted a real-world flight experiment.
At that stage, the simulator was still in an early phase of development; actuator dynamics and aerodynamic drag were not yet modeled, and policy training relied on ground-truth state information.

As a result, there was a significant mismatch between the simulator and real flight conditions.
This led to poor trajectory tracking performance, with position and velocity \ac{RMSE} values of $6.11~\mathrm{m}$ and $4.25~\mathrm{m/s}$, respectively.
Although the overall performance was limited, \autoref{fig:real-world} shows that the general shape of the trajectory was still preserved.
This suggests that \ac{DR} contributed positively to policy transferability in real-world conditions.
Additionally, the experiment provided valuable insights and data that were later used to improve the simulator.
% RMSE: 6.11020 m | RMSE: 4.25100 m  | Max Speed: 8.11 m/s
\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/uzh-real-flight/hypotheses.pdf}
    \caption{Real world flight data, UZH track trajectory tracking.}
    \label{fig:real-world}
\end{figure}



\section{Model-Free and Model-Based evaluation}\label{sec:baseline}
In this section, we compare two policies, one optimized using model-free \ac{RL} with \ac{PPO}, and the other using model-based \ac{RL} with \ac{BPTT}.
The comparison is performed in terms of sample efficiency, mean reward, performance, transferability, and robustness.
\subsection{Training Time}
To evaluate the training time we measure the wall time agians the number of steps.
It can be observed that the relationship between the steps and time is linear.
Taking for \ac{BPTT} \textbf{7x} times less clock time to execute the same number of time steps than \ac{PPO}.
\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/sample-eff/hypotheses.pdf}
    \caption{Wall time versus training steps for \ac{PPO} and \ac{BPTT}. }
\end{figure}
It is worth mentioning that the wall time is highly dependant on the frequency at which the network is optimized, as oftelny this procedure is the most time consuming.
% For the model-free policy, backpropagation is produced every 100 steps, and for the model-based 
For this comparison, the policies are optimized when losses for 50 stpes for 1024 parallel environments are collected, that is to say, every 51200 stpes. 

\subsection{Discrete and Continuous Rewards}
\todo{}
\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/rewards/hypotheses.pdf}
    \caption{Comparison between BPTT and PPO when the loss is countinuous or discrete.}
\end{figure}

\subsection{Performance}

\begin{equation}
    \mathbf{p}(\theta) = \left( -\text{scale} \cdot \sin(\theta)\cos(\theta), \ -\text{scale} \cdot \cos(\theta), \ \text{h} \right)
\end{equation}
where     $\theta = \omega \cdot t \cdot dt$  and $\omega = \text{speed}/\text{scale}$
\begin{figure}[H]
    \centering
    \includegraphics{fig/plots/fig-8/hypotheses.pdf}
    \caption{\todo{}}
\end{figure}
\subsection{Transferability and Robustness}



% \begin{table}[H]
%     \centering
%     \large
%     \caption{Performance comparison of policies trained with differentiable gradients versus PPO, evaluated in the proposed simulator and Gazebo environment.}
% \begin{tabular}{lll|ll}
% \toprule
% \multicolumn{1}{c}{} & \multicolumn{2}{c|}{BPTT}          & \multicolumn{2}{c}{PPO}           \\ \hline
%                        & \multicolumn{1}{c}{Ours} & Gazebo & \multicolumn{1}{c}{Ours} & Gazebo \\ \hline
% $N$                      & \multicolumn{1}{c}{}     &        & \multicolumn{1}{c}{}     &        \\ 
% $T_t$                 & \multicolumn{1}{c}{}     &        & \multicolumn{1}{c}{}     &        \\ 
% $SR$                     & \multicolumn{1}{c}{}     &        & \multicolumn{1}{c}{}     &        \\ 
% $T_f$                   & \multicolumn{1}{c}{}     &        & \multicolumn{1}{c}{}     &        \\ \bottomrule
% % Transfer               & \multicolumn{2}{c|}{Good}          & \multicolumn{2}{c}{Good}          \\ \hline
% \end{tabular}
% \end{table}


% \begin{figure}[H]
%     \centering
%     \includegraphics[width=0.8\textwidth]{fig/photos/placehldrs/vel_dist.png}
%     \caption{\todo{This is a placeholder figure, (I need to get the actual data)}Wind response, gazebo?}
%     \label{fig:wind}
% \end{figure}